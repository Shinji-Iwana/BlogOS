# BlogOS Claude Code Instructions

**バージョン:** 2.0.0
**最終更新日:** 2026-09-24
**主な変更:** 2026-09-23 設計監査の決定事項（`docs/BLOGOS_DECISIONS.md`）を反映。文書の担当範囲・優先順位・監査結果の分類を本書に一本化し、他の文書と重複する規則を参照に置き換えた（D-12-01〜D-12-03）

## 1. 本書の目的

本書は、Claude Code（**開発支援AI**）がBlogOSを調査・設計・実装・修正する際の**作業手順の基準**である。

本書は仕様を定義しない。仕様は各設計書で定義する。本書が定義するのは次の事項である。

* 設計書の担当範囲と、設計書同士の優先順位（3章）
* 設計書を読む順序（4章）
* 現在の実装と設計の差異の扱い（5〜6章）
* 監査結果の分類（6-2）
* 作業の進め方・確認・報告（7〜10章）
* 判断に迷ったとき・矛盾を見つけたときの扱い（11〜12章）

本書でいう「AI」は開発支援AIを指す。BlogOSに組み込むAI機能（**BlogOSのAI機能**）とは区別する（D-07-09）。

---

# 2. BlogOSの基本認識

BlogOSは、複数のWordPressブログを一元的に管理・分析・改善するLaravelアプリケーションである。単なるWordPress管理画面の代替ではなく、WordPressから取得した情報をBlogOSのDBに保持し、DBを参照元として、記事管理・Google関連データ・記事評価・BlogOSのAI機能による記事支援・WordPressへの反映・履歴管理を行う。

BlogOSは、機能・ブログ・データを今後も追加していく継続的なシステムとして扱う。

動作環境はXServer、利用者は1人（ログイン必須）、PCとスマートフォンの両方で利用する（REQUIREMENTS 13章）。

---

# 3. 設計書の担当範囲と優先順位

## 3-1. 担当範囲と優先順位

（D-12-01）

| 優先度 | 文書 | 担当範囲 |
| --- | --- | --- |
| 1 | `docs/BLOGOS_REQUIREMENTS.md` | 何を実現するか |
| 2 | `docs/BLOGOS_ARCHITECTURE.md` | 構造、データの流れ、参照元と正本、AI機能の段階、用語集 |
| 3 | `docs/BLOGOS_DATABASE.md` | データの保存方法（ID命名、履歴、削除、保存期間） |
| 3 | `docs/BLOGOS_WORDPRESS_API.md` | WordPressとの通信方法（取得条件、差分判定、反映手順） |
| 4 | `docs/BLOGOS_DEVELOPMENT_RULES.md` | 実装の方法（開発時の優先順位、エラー処理、セキュリティ、テスト、Git） |
| 別枠 | 品質基準 | 記事の品質だけを扱う。システムの仕様とは優先を比べない |
| 別枠 | 本書（CLAUDE.md） | Claude Codeの作業手順。仕様は定義しない |
| 例外 | WordPress・Laravel等の公式仕様 | それらの製品の挙動に関する事実は、公式仕様を優先する |

* DATABASE と WORDPRESS_API は同格とし、互いの担当範囲に立ち入らない。
* 品質基準の正本は `resources/quality/`（共通基準とブログ別の定義）とする。`docs/BLOGOS_QUALITY_STANDARD.md` は案内だけを残している（D-06-10、D-07-05）。

## 3-2. 設計書の外にある記録

| 文書 | 役割 |
| --- | --- |
| `docs/BLOGOS_DECISIONS.md` | 設計上の決定の記録（いつ・なぜ決まったか）。各設計書の `（D-xx-xx）` はここを指す |
| `docs/BLOGOS_CURRENT_STATUS.md` | 現在の実装の状態の記録。理想設計を定義するものではない |

## 3-3. 一つの規則は一か所で定義する

（D-12-03）

| 規則 | 定義する場所 |
| --- | --- |
| 参照元と正本、WordPress APIを直接呼んでよい場面、AI機能の段階、用語 | ARCHITECTURE（3-2、3-4、18-2、29章） |
| ID・列の命名、履歴、削除、保存期間 | DATABASE（3章、8章、13章、14章） |
| 取得条件、差分判定、反映手順、WordPress側の拡張 | WORDPRESS_API（第III〜V部） |
| 開発時の優先順位、エラー処理、セキュリティ、テスト、Git、クラス・ルート等の命名（DBの列名はDATABASE） | DEVELOPMENT_RULES（2章、5章、12〜17章） |
| 文書の優先順位、現在の実装との関係、監査結果の分類 | 本書 |

設計書を修正するときは、同じ規則を複数の文書に書かず、定義している場所を節番号で参照する。

---

# 4. 設計書を読む順序

初めてBlogOSを調査する場合は、次の順に読む。

1. 本書（CLAUDE.md）
2. BLOGOS_REQUIREMENTS.md
3. BLOGOS_ARCHITECTURE.md
4. BLOGOS_DATABASE.md
5. BLOGOS_WORDPRESS_API.md
6. BLOGOS_DEVELOPMENT_RULES.md
7. BLOGOS_DECISIONS.md（決定の経緯が必要な場合）
8. BLOGOS_CURRENT_STATUS.md（存在する場合）
9. 品質基準（`resources/quality/README.md` から。記事の品質・BlogOSのAI機能に関わる作業の場合）
10. 現在のソースコード、Migration、Routes、Views、Tests、設定

特定の機能だけを扱う場合は、必要な設計書を選んで読んでよい。ただし、3-3 の「定義する場所」は必ず確認する。

---

# 5. 最重要原則

## 5-1. 設計と現在の実装を混同しない

現在のコードは、理想設計を満たしているとは限らない。「コードに存在する = 正しい仕様」とは考えない。現在の実装には、未完成の処理、暫定実装、設計変更前の実装、不要になった処理、重複、バグが含まれている可能性がある。

**現在のコードは、理想設計に対して評価・修正される対象である。**

## 5-2. 設計書を現在のコードに合わせて変更しない

現在の実装が設計書と異なる場合、コードに合わせて設計書を書き換えない。まず、理想設計・現在の実装・差異・差異の理由・修正の要否を整理する。

一方で、理想設計を無条件に正しいものとして実装を変更するのでもない。差異を明確にし、人間が判断してから、設計または実装を変更する。

## 5-3. 推測で仕様を決めない

設計書・コード・設定・DB構造・テスト・実際のAPIレスポンスから確認できない仕様を、勝手に決めない。不明な事項は「不明」「未決定」「確認が必要」「実装からは判断できない」として扱う。

特に、DB構造、API仕様、認証方式、データの保存・削除の方針、権限、外部サービスとの連携方法、既存処理の意図は、推測で決めない。

---

# 6. 現在の実装との差異

## 6-1. 差異の扱い

現在の実装が設計書と異なる場合は、次の順で扱う。

1. 現在の実装を確認する
2. 設計書との違いを明確にする
3. 差異の原因を確認する
4. 影響範囲を確認する
5. 対応案を整理する
6. 人間が判断する
7. 設計または実装を変更する

判断の観点：仕様上必要か、設計上必要か、現在の実装に必要な理由があるか、削除すると既存機能に影響するか、修正の影響範囲は何か。

「古いコードだから削除する」「今動いているから残す」という理由だけで判断しない。

## 6-2. 監査結果の分類

設計と実装を比較した結果は、次の分類で整理する（D-12-02）。`BLOGOS_CURRENT_STATUS.md` でも同じ分類を使う。

| 分類 | 意味 | 例 |
| --- | --- | --- |
| 一致 | 設計どおりに実装されている | ― |
| 不足（未実装） | 設計上必要だが、実装されていない | 同期の実行記録がない |
| 相違 | 設計と実装で仕様が異なる | 認証情報を .env で管理している |
| バグ | 設計とは関係なく、実装として誤っている | null処理不足、N+1、ルートの不整合 |
| 過剰（不要・重複） | 設計上不要な処理、重複した処理 | 同じAPIを複数のServiceから直接呼んでいる |
| 設計上の懸念 | 動作はするが、今後の拡張で問題になる構造 | 巨大なController、責務の混在 |
| 要確認 | 設計書・実装から判断できない | ― |

問題の優先度は、セキュリティ → データ整合性 → 重大なバグ → 機能のバグ → 設計との不一致 → パフォーマンス → 保守性 → UI の順で付ける。

## 6-3. BLOGOS_CURRENT_STATUS.md

現在の実装の状態を整理する場合は `docs/BLOGOS_CURRENT_STATUS.md` を使う。このファイルは、現在のコードを調査した後に作成・更新する。理想設計ではないため、設計書を書き換える根拠として使ってはならない。

---

# 7. 監査と実装を分ける

## 7-1. 監査

目的は、現在のBlogOSが理想設計に対してどのような状態かを把握することである。監査の段階では、大規模なコード変更を行わない。

監査の手順：

設計書の確認
↓
プロジェクト構造の確認
↓
主要コード・DB・Routes・Views・Tests・外部API連携の確認
↓
設計との差異の整理（6-2 の分類）

## 7-2. 実装

監査結果を人間が確認した後に、修正対象・修正方法・優先順位を決めて実装する。

実装は、一度に大量に行わず、次の流れで進める。

設計の確認
↓
変更単位の決定
↓
実装
↓
テスト
↓
結果の確認
↓
次の変更

実装時には、変更内容・変更理由・影響範囲・対象ファイル・テスト方法を明確にする。

---

# 8. 実装前の確認

コードを変更する前に、関連する既存の実装を確認する。1ファイルだけを見て修正し、周辺の処理との整合性を壊してはいけない。

最低限、次を確認する。

変更対象のファイル（全体）
↓
呼び出し元・呼び出し先
↓
関連するModel・DB・Migration
↓
関連するRoute・View
↓
関連するService・Repository・DTO・API Client
↓
関連するTest

既存ファイルを変更する場合は、ファイル全体（import、constructor、プロパティ、メソッド、validation、戻り値、例外、コメント）を確認してから変更する。

新しいファイルを作る場合は、同じ責務のファイルが既にないか、既存クラスで対応できないか、命名規則・ディレクトリ構成（DEVELOPMENT_RULES 5章）に合っているかを確認する。

---

# 9. 変更時の原則

* 依頼された変更と、設計上必要な変更だけを行う。デザイン・UI・命名の変更、大規模なリファクタリング、不要なライブラリの追加、ファイルの移動を「ついでに」行わない。
* 無関係な既存処理を壊さない。
* ただし、変更対象が1ファイルでも、設計上必要な関連修正（Model → Migration → Repository → Service → Controller → View → Test）は確認する。
* 設計に存在しない仕様を勝手に追加しない。
* 実装ルールの詳細は `BLOGOS_DEVELOPMENT_RULES.md` に従う。

---

# 10. 変更後の確認と報告

## 10-1. 確認

変更内容に関係する範囲で、次を確認する。

□ 設計との整合性
□ 関連コードとの整合性
□ Route・Controller・Service・Repository・Model・DTO・Migration・View・JavaScript
□ DB・外部API
□ エラー処理・バリデーション
□ テスト（`BLOGOS_DEVELOPMENT_RULES.md` 16章）

テストが不足している場合、勝手に大量のテストを追加する必要はない。ただし、重要な機能、データを壊す可能性のある処理、外部API連携、同期、認証、複雑な業務ロジックについては、不足しているテストを報告する。

## 10-2. 報告

変更したファイル、変更内容、変更理由、テスト結果、未解決事項を報告する。実装できなかったこと、判断できなかったことを隠さない。

---

# 11. 矛盾・未決定事項の扱い

## 11-1. 設計書同士の矛盾

設計書同士の矛盾を見つけた場合は、勝手にどちらかを採用しない。

* 3-3 で「定義する場所」が決まっている規則は、その文書の記述を正とする（他の文書の反映漏れとして報告する）。
* それ以外は、3-1 の優先順位を参考にしつつ、矛盾を報告して人間が判断できる状態にする。他の文書から明確に正解が分かる場合は、その根拠を示す。

## 11-2. 未決定事項

設計書に「未決定」と書かれている事項を、勝手に確定しない。必要な場合は、選択肢・それぞれの長所と短所・影響を整理し、推奨案があれば根拠を付けて示し、人間の判断を待つ。

未決定事項の一覧は `BLOGOS_DECISIONS.md` 3章と、各設計書の未決定事項の章にある。

## 11-3. 決定を記録する

人間が設計上の判断をした場合は、`BLOGOS_DECISIONS.md` に記録し（既存の記録は書き換えず追記する）、関係する設計書を更新して、冒頭のバージョンと最終更新日を更新する。

設計書を更新するのは、仕様が変わった場合だけとする。単なる実装の修正で、設計書をコードに合わせて書き換えない。

---

# 12. 人間の確認が必要な変更

次のような変更は、勝手に実施せず、確認を求める。

* DBのデータを大量に削除する処理
* 本番データを壊す可能性のある処理
* 大規模なアーキテクチャの変更
* 認証方式の変更
* 外部APIとの連携の重要な変更
* 既存機能の削除
* 既存データの移行方法の大きな変更
* 設計書の大幅な変更
* 本番のWordPressへの作成・更新・削除・公開

---

# 13. セキュリティ上の注意

パスワード、APIキー、Secret、アクセストークン、リフレッシュトークン、OAuthやサービスアカウントの認証情報、Cookie、秘密鍵を、ソースコード・Git・ログ・画面に出さない。`.env` をGitの管理対象にしない。

既存のコードに秘密情報が含まれているのを見つけた場合は、実装を進める前に問題として報告する。

詳細なルールは `BLOGOS_DEVELOPMENT_RULES.md` 13章に従う。

---

# 14. 最終原則

設計を理解する。
↓
現在の実装を調査する。
↓
設計と実装を比較する。
↓
差異を明確にする。
↓
必要な変更だけを行う。
↓
変更を検証する。
↓
設計と実装の整合性を維持する。

特に重要なのは、次の2点である。

> **現在のコードをBlogOSの仕様そのものとみなさないこと。**

> **理想設計を現在のコードに合わせて安易に変更しないこと。**

Claude Codeは、BlogOSのコードをただ動かすだけではなく、「要求・設計・DB・API・実装・テストが一貫したシステムになっているか」という観点でBlogOS全体を扱うこと。
